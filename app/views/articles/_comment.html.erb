<div class="comment">
	<p>Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later
	<h4>Comment by <%= comment.author_name %></h4>
	<p><%= comment.body %></p>
</div>