<h1>All Resources</h1>
<ul>
	<% @articles.each do |article| %>
	<li>
		<%= link_to article.title, article_path(article) %>
<p>Tags: <%= tag_links(article.tags) %></p>
	</li>
	<% end %>
	<%= link_to "Add a New Resource", new_article_path, :class => "new_article" %>
</ul>
